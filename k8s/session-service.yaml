---
# kubectl create secret generic session-service-secret \
#   --from-literal=SUPABASE_URL=https://tulwescncreiuclmfafv.supabase.co \
#   --from-literal=SUPABASE_SECRET_KEY=your_secret_key_here

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: default
  labels:
    app: session-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: session-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: session-service
    spec:
      containers:
        - name: session-service
          image: nahnahsylvestre/skillbridge-session-service:latest
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              value: "3003"
            - name: NODE_ENV
              value: "production"
            - name: KAFKA_BROKER
              value: "skillbridge-kafka:9092"
            - name: SUPABASE_URL
              valueFrom:
                secretKeyRef:
                  name: session-service-secret
                  key: SUPABASE_URL
            - name: SUPABASE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: session-service-secret
                  key: SUPABASE_SECRET_KEY
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: default
  labels:
    app: session-service
spec:
  selector:
    app: session-service
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
  type: ClusterIP